#include "main.h"
/**
 * add - it can be add tow numbers
 * @a: the first number
 * @b: the second number
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	int s;

	s = a + b;
	return (s);
}
